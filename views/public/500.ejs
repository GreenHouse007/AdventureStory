<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
    <meta name="robots" content="noindex" />
  </head>
  <body>
    <%- include('../partials/header', { user }) %>

    <main class="container">
      <section class="empty-state">
        <h1 class="title">Something broke (500)</h1>
        <% const fallbackMessage = 'An unexpected error occurred.'; %>
        <p class="muted">
          <%= typeof message !== 'undefined' ? message : fallbackMessage %>
        </p>
        <% const fallbackBackLink = backLink || (user ? '/u/library' : '/'); %>
        <a class="cta-button" href="<%= fallbackBackLink %>">Back to where you were</a>
      </section>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>
