<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
    <meta name="robots" content="noindex" />
  </head>
  <body>
    <%- include('../partials/header', { user }) %>
    <main class="container">
      <section class="empty-state">
        <h1 class="title">403 — Forbidden</h1>
        <p class="muted">You don’t have access to this path.</p>
        <% const fallbackBackLink = backLink || (user ? '/u/library' : '/'); %>
        <a class="cta-button" href="<%= fallbackBackLink %>">Go back</a>
      </section>
    </main>
    <%- include('../partials/footer') %>
  </body>
</html>
